// O array é um objeto que contém índices numéricos e elementos, que podem ser de qualquer tipo primitivo de dados ou objetos (o que inclui outros arrays).

const nomes = ["Saitama", " Goku", "Vegeta", "Thor"];

console.log(`O array é: ${nomes}`);

console.log(`O item na primeira posição do array é: ${nomes[0]}`);

console.log(`O array possui um total de ${nomes.length} itens`);
