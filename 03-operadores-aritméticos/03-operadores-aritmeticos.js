// Operadores de incremento e decremento:

let a = 5;
a = a + 1;
a = a - 1;

// Simplificação dos operadores de incremento e decremento:

let b = 5;
b += 1;
b -= 1;

// Segunda simplificação dos operadores de incremento e decremento:

let c = 5;
c++;
c--;
