// Hoisting também é um conceito muito importante que temos entender antes de falar sobre escopos no JavaScript

// Quando declaramos uma variável ou uma função no JavaScript e rodamos o nosso código, ocorre um fenômeno chamado Hoisting. Antes de executar qualquer parte do código o JavaScript eleva a declaração das variáveis e funções para o topo do código, quando isso ocorre as variáveis não recebem valor, estando com o valor undefined (var), porém são declaradas. Isso permite com que possamos usar as variáveis antes da sua declaração.
